# Dev/CI-only dependencies. NOT installed on the deployed app.
# Install locally with:  pip install -r requirements.txt -r requirements-dev.txt
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0
pydantic>=2.0.0
